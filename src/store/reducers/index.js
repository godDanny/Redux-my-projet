//3. reducer是纯函数
// 两个参数： state（初始状态）  action
export const numReducer = (state=1,action) => {
    switch (key) {
        case value:
            
            break;
    
        default:
            break;
    }
    // return一个新的state
    return state
}